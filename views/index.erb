<%= form( "/complete", :post ) %>

  テキストを入力してください
    <div class="text_input">
      <%= textarea :text, :content %>
    <div>

  <%= radio :text, :wordclass, %w(名詞 動詞 形容詞 副詞 助詞 助動詞 記号) %>

<%= submit %>
